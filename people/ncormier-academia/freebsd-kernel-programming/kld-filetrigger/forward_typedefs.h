/*
** forward_typedefs.h for  in /home/nico/lang/kernel/kld-filetrigger
** 
** Made by nicolas
*/

#ifndef __FORWARD_TYPEDEFS_H__
# define __FORWARD_TYPEDEFS_H__

typedef struct elem		elem_t;
typedef struct dist_elem	dist_elem_t;
typedef struct host		host_t;
typedef struct server		server_t;
typedef struct file		file_t;
typedef struct msocket		socket_t;

#endif /* __FORWARD_TYPEDEFS_H__ */
