This is sample code from cairographics.org with some modifications

The binaries produce a PNG. You can use eog or a browser to view them.

arc     - draw an arc over a transparent background
arc-bg  - draw an arc over an opaque background
stroke  - draw the edges of a rectangle (cairo_stroke)
fill    - draw a filled rectangle (cairo_fill)
